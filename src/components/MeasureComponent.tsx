import type { Measure } from "../types/create"

interface MeasureProps{
    measure:Measure;
}
function MeasureComponent({measure}:MeasureProps) {
    return(
        <>
        </>
    )
}
export default MeasureComponent